# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    env:
      NEXT_PUBLIC_APP_NAME: mhmarket
      NEXT_PUBLIC_API_PRODUCTION : https://mhmarket.pk/api
      # NEXT_PUBLIC_GOOGLE_APP_ID : 233419149658-e84jj0d08c23u3qibev3skjvirhd10sv.apps.googleusercontent.com
      NEXT_PUBLIC_SHARE_NUMBER: 3135234626
    
    runs-on: self-hosted

    # strategy:
    #   matrix:
    #     node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    # - name: Use Node.js ${{ matrix.node-version }}
    #   uses: actions/setup-node@v3
      # with:
      #   node-version: ${{ matrix.node-version }}
      #   cache: 'npm'
    - run: | 
        export NODE_OPTIONS="--max_old_space_size=4096"
        echo "working"
        npm ci --force
        npm run build
        pm2 stop npm 
        pm2 start npm